{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}!{% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>
    <img src="{{ asset('uploads/') ~ post.image }}" alt=""/>
    <div>
        {{ post.content }}
    </div>
{% endblock %}
